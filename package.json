{
  "name": "logseq-logtero-plugin",
  "version": "1.0.1",
  "description": "Create pages for Zotero items using custom page titles and properties",
  "main": "dist/index.html",
  "targets": {
    "main": false
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-source-maps src/index.html --public-url ./"
  },
  "keywords": [],
  "author": "vyleung",
  "license": "MIT",
  "dependencies": {
    "@logseq/libs": "^0.0.6",
    "driftless": "^2.0.3",
    "fuse.js": "^6.6.2"
  },
  "devDependencies": {
    "parcel": "^2.5.0"
  },
  "logseq": {
    "id": "logseq-logtero-plugin",
    "title": "Logtero",
    "icon": "./icon.png"
  }
}
